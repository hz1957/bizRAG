# Milvus Embedding and Indexing Demo for UltraRAG UI

# MCP Server
servers:
  retriever: bizrag/src/servers/retriever

# MCP Client Pipeline
pipeline:
- retriever.retriever_init
- retriever.retriever_index
